---
title: Downloads & Version
description: Download DeepSeek Harness in Practice as PDF, EPUB or Markdown, and check the current site version.
---

import { DOWNLOADS, VERSION, REPO_URL } from '../../../consts';

export const base = import.meta.env.BASE_URL;
export const ISSUES = `${REPO_URL}/issues/new`;

<dl class="stb-version">
  <div><dt>Version</dt><dd>v{VERSION.version}</dd></div>
  <div><dt>Commit</dt><dd><code>{VERSION.commit}</code></dd></div>
  <div><dt>Content updated</dt><dd>{VERSION.updatedAt.slice(0, 10)}</dd></div>
  <div><dt>Built</dt><dd>{VERSION.builtAt.slice(0, 16).replace('T', ' ')} UTC</dd></div>
</dl>

## Downloads

<p class="stb-note">The book content itself is in Chinese. The PDF and EPUB below are generated from the same Chinese manuscript; only the site chrome is translated.</p>

<ul class="stb-download-list">
  {DOWNLOADS.map((d) => (
    <li>
      <a href={`${base}${d.file}`}>{d.name}</a>
      <span class="stb-download-note">{d.note}</span>
    </li>
  ))}
</ul>

> The EPUB is generated by CI with pandoc on every build. If the layout looks wrong, <a href={ISSUES}>open an issue</a>.

## Licence

- Text: [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/) (attribution)
- Companion code: <a href={`${REPO_URL}/blob/main/LICENSE`}>MIT</a>
