---
title: 下载与版本
description: 下载《DeepSeek Harness 应用开发实践》的 PDF、EPUB、Markdown，并查看当前站点版本与更新记录。
---

import { DOWNLOADS, VERSION, REPO_URL } from '../../consts';

export const base = import.meta.env.BASE_URL;
export const ISSUES = `${REPO_URL}/issues/new`;

<dl class="stb-version">
  <div><dt>版本</dt><dd>v{VERSION.version}</dd></div>
  <div><dt>提交</dt><dd><code>{VERSION.commit}</code></dd></div>
  <div><dt>内容更新</dt><dd>{VERSION.updatedAt.slice(0, 10)}</dd></div>
  <div><dt>本次构建</dt><dd>{VERSION.builtAt.slice(0, 16).replace('T', ' ')} UTC</dd></div>
</dl>

## 下载

<ul class="stb-download-list">
  {DOWNLOADS.map((d) => (
    <li>
      <a href={`${base}${d.file}`}>{d.name}</a>
      <span class="stb-download-note">{d.note}</span>
    </li>
  ))}
</ul>

> EPUB 由 CI 在每次构建时用 pandoc 从同一份书稿生成。如果排版有问题，<a href={ISSUES}>提个 Issue</a> 告诉我。

## 许可

- 文字内容：[CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)（署名）
- 配套代码：<a href={`${REPO_URL}/blob/main/LICENSE`}>MIT</a>
